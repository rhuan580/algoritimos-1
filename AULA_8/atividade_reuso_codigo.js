// Foi solicitado para que você faça o mesmo procedimento feito com a formula de area
// só que dessa vez você vai fazer com a area do triangulo.
// 1:. Pesquisar a formula da area do triangulo.
// 2:. Definir uma function chamada calcularAreaTriangulo
// 3:. Colocar um console.log(calcularAreaTriangulo(10))

function calcularAreaTriangulo(base, altura) {
  const area = (base * altura) / 2;
  return area;
}
console.log(calcularAreaTriangulo(10, 20));
console.log(calcularAreaTriangulo(10, 20));
console.log(calcularAreaTriangulo(10, 20));
